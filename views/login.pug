extends layouts/main

block content
    .form-container
        form(action=routes.login, method="post")
            input(type="email", mane="email",placeholder="Email")
            input(type="password", mane="password",placeholder="password")
            input(type="submit", value="Log In")